class InventoryModel {
  constructor (id, skuId, sku, type, amount) {
    this.id = id
    this.skuId = skuId
    this.sku = sku
    this.type = type
    this.amount = amount
  }
}
export default InventoryModel