class InventoryModel {
  constructor (id, date, sku, type, amount) {
    this.id = id
    this.date = date
    this.sku = sku
    this.type = type
    this.amount = amount
  }
}
export default InventoryModel